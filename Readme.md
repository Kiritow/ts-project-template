# TS Template Project

Install Basic Dependencies (tsc and eslint)

```bash
npm install -D typescript tsc-alias eslint eslint-config-prettier eslint-plugin-prettier jiti prettier typescript-eslint @eslint/js
```

Alternatively, install `@typescript/native-preview` for `tsgo`

```bash
npm install -D @typescript/native-preview
```
